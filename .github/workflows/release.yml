name: Publish compiler builds

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04 # older glibc = compiler runs on more distros
            slug: linux-amd64
          - os: windows-latest
            slug: windows-amd64
          - os: macos-latest
            slug: macos-arm64
          - os: macos-15-intel
            slug: macos-amd64
          - os: ubuntu-22.04-arm
            slug: linux-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      # Linux/macOS builds link libLLVM dynamically — consumers of these
      # artifacts need the matching LLVM runtime (libllvm20 / brew llvm@20).
      - name: Install LLVM (Linux)
        if: runner.os == 'Linux'
        run: wget -qO- https://apt.llvm.org/llvm.sh | sudo bash -s -- 20

      - name: Install LLVM (macOS)
        if: runner.os == 'macOS'
        # Pin LLVM_CONFIG explicitly — build_odin.sh otherwise grabs the
        # highest brew llvm on the runner and the runtime dep drifts.
        run: |
          brew list llvm@20 &>/dev/null || brew install llvm@20
          echo "LLVM_CONFIG=$(brew --prefix llvm@20)/bin/llvm-config" >> "$GITHUB_ENV"

      - name: Set up MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build (Linux/macOS)
        if: runner.os != 'Windows'
        run: ./build_odin.sh release

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: call build.bat release

      - name: Package
        shell: bash
        run: |
          mkdir staging
          if [ "${{ runner.os }}" = "Windows" ]; then
            # -L: vendor ships .so symlinks that bash cp on Windows can't
            # recreate — copy the link targets instead.
            cp -rL base core vendor LICENSE staging/
            # odin.exe loads LLVM-C.dll from its own directory at runtime
            cp odin.exe LLVM-C.dll staging/
          else
            cp -r base core vendor LICENSE staging/
            cp odin staging/
          fi
          git rev-parse HEAD > staging/COMMIT
          tar -czf "methodin-${{ matrix.slug }}.tar.gz" -C staging .

      - name: Upload to rolling "latest" release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        # The three platform jobs race to create the release; losers fall
        # through to the upload, which overwrites the previous build.
        run: |
          gh release create latest --prerelease --title "Latest master build" \
            --notes "Rolling compiler build of master; assets are overwritten on every push. See COMMIT inside each archive for the exact revision." \
            || true
          gh release upload latest "methodin-${{ matrix.slug }}.tar.gz" --clobber
